public class Solution {
    public int missingNumber(int[] nums) {
        int len = nums.length;
        int expect =0;
        int real = 0;
        expect = (0+len)*(len+1)/2;
        for (int i = 0; i<len;i++) real = real+nums[i];
        return expect-real;
        
    }
}
